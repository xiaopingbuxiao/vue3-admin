import { createStore, } from 'vuex'

import globalState from './modules/globalState'

export default createStore({
  state: {
  },
  mutations: {
  },
  actions: {
  },
  modules: {
    globalState,
  },
})
